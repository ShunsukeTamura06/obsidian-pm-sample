---
作成日: 2026-04-22
作成者: 田中太郎
---

# 週次報告 2026-W17

## サマリ

経費精算システム刷新の要件定義が著手。AI業務活用案件はSymphony連携テスト中。ワークフロー承認電子化はシステム設計フェーズに入り、各担当が安定進行中。

## 進行中案件

```dataview
TABLE 担当, 優先度, 期限
FROM "01_案件"
WHERE 種別 = "案件" AND ステータス = "進行中"
SORT 優先度 DESC
```

## 進行中タスク

```dataview
TABLE file.link AS タスク, 案件, 担当, 期限, 工数進捗 + "%" AS 進捗
FROM "01_案件"
WHERE 種別 = "タスク" AND ステータス = "進行中"
SORT 期限 ASC
```

## 要対応（期限14日以内）

```dataview
TABLE file.link AS ノート, 種別, 担当, 期限
FROM "01_案件"
WHERE (種別 = "案件" OR 種別 = "タスク")
    AND ステータス != "完了"
    AND date(期限) <= date(today) + dur(14 days)
SORT 期限 ASC
```

## 担当者別ロード

```dataview
TABLE WITHOUT ID 担当 AS 担当者, length(rows) AS 引き受けタスク
FROM "01_案件"
WHERE 種別 = "タスク" AND ステータス != "完了"
GROUP BY 担当
SORT length(rows) DESC
```

## リスク・課題

- 経費精算要件定義：現行フローの自動化範囲確定にベンダーと認識差异あり。来週中に調整MTGを設定予定。

## 来週の重点

- 経費精算要件定義：自動化範囲の確定
- AI業務活用：Symphony連携テスト完了
- ワークフロー：稟議・発注承認フロー設計著手
