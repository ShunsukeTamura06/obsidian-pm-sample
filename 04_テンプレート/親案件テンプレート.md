---
担当: 
案件種別: 
ステータス: 進行中
優先度: 中
開始日: 
期限: 
案件レベル: 親
tags: [cluster/]
---

## 概要



## 子案件一覧

```dataview
TABLE file.link AS 子案件, 担当, ステータス, 期限, 工数進捗 + "%" AS 進捗
FROM "01_案件/{{title}}"
WHERE 親案件 = this.file.link
SORT 期限 ASC
```

## 子案件の集計

```dataview
TABLE WITHOUT ID length(rows) AS 子案件数, length(filter(rows.ステータス,(s)=>s="完了")) AS 完了, length(filter(rows.ステータス,(s)=>s="進行中")) AS 進行中, round(sum(rows.工数進捗)/length(rows))+"%" AS 平均進捗
FROM "01_案件/{{title}}"
WHERE 親案件 = this.file.link
GROUP BY true
```

## 担当者



## 子案件リンク


